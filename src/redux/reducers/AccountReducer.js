const intState ={account : null, accounts :[]}
const AccountReducer = (state =intState, action)=>{

    switch(action.type){
        default:
            return state
    }
}
export default AccountReducer;